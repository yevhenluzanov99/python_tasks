# Django Project

## Описание

Этот проект использует Django и включает:

- Модель `Company`, которая ограничивает количество экземпляров до одного.
- Модели `Student` и `Course` с добавленными новыми полями.
- Регистрация модели `Student` в админ-панели.